%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:ages, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:ages, :new)

.tabs-content
  -form_for :age, url(:ages, :create), :class => 'form-horizontal' do |f|
    =partial 'ages/form', :locals => { :f => f }
